import React from 'react'

await addDoc(collection(db, "collectionID"), {field:value, field:value});
  const snap = await getDocs(collection(db, "collectionID"));


export default function Messenger({email,db}) {
  return (
    <div className='messenger'>
        <input type="text" className='uzenet' />
    </div>
  )
}
